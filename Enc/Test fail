from microbit import *
import radio

radio.config(group=5)
radio.on()

while True:
    if button_a.is_pressed():
        radio.send("forwards")
        display.show("A")
    elif button_b.is_pressed():
        radio.send("stop")
        display.show("B")
    else:
        display.clear()
    sleep(100)
