#Test 1: Unencrypted Attacker
#Sends plain text "forwards" / "stop" on group 5
#Expected result: Vehicle rejects all commands
from microbit import *
import radio

radio.config(group=5)
radio.on()

count = 0

while True:
    if button_a.was_pressed():
        radio.send("forwards")
        count += 1
        display.show(str(count % 10))
        sleep(200)
    elif button_b.was_pressed():
        radio.send("stop")
        count += 1
        display.show(str(count % 10))
        sleep(200)
    sleep(50)# Write your code here :-)
